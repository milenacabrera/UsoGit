0.1 ABRIR CONSOLA

WINDOWS + R
	cmd

1. Descargar e instalar Git (UNA SOLA VEZ).
1.1. Abre el enlace: https://git-scm.com/downloads
1.2. Selecciona sistema operativo (Windows).
1.3. Descarga e instala Git.

2. Crear cuenta en GitHub (YA).

3. Bajar archivos de la nube.
3.1. Abre el repositorio en su cuenta de GitHub.
3.2. Crea una carpeta en su PC (ubicación de preferencia).
3.3. Copia la dirección de la carpeta.
3.4. Abre consola.
3.5. Acceder a la dirección de la carpeta:
	cd <dirección>. P. Ej: cd C:\Users\juand\Desktop\Bla
3.6. Inicializar repositorio local:
	git init
3.7. Entrelazar repositorio web con local:
	git remote add origin <enlace repositorio GitHub>
3.8. Cargar archivos desde la nube:
	git pull origin master

4. Antes de trabajar -> Actualizar conexión web-local (RECOMENDACIÓN):
4.1. Abre el repositorio local (carpeta).
4.2. Cersiorese del estado:
	git status
4.3. Importa archivos web:
	git pull origin master

5. Subir archivos a GitHub.
	NOTA: Los archivos deben pesar menos de 50 MB.
5.1. Abre el repositorio local (carpeta).
5.2. Cersiorese del estado:
	git status
5.3. Añade a la cola de envío (todos los archivos nuevos):
	git add .
5.4. Añadir comentario de actualización (necesario):
	git commit -m "COMENTARIO"
5.5. Subir a GitHub:
	git push origin master
